{
  "buildCommand": "npx prisma generate --no-engine && npx prisma db seed && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"]
}
